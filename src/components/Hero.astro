---

---

<section>
  <div class="card">
    <h1>
      Buscando una nueva casa
    </h1>
    <h2>
      Venda o compre su casa con un agente de bienes raíces independiente que se enfoca en encontrar su hogar ideal
    </h2>
  </div>
  <img src="./house-2.jpeg" alt="house-1" />
</section>
<style>
  section {
    position: relative;
    height: 100vh;
  }
  h1 {
    font-size: 5rem;
    font-weight: 400;
    color: #fff;
    text-align: center;
  }
  h2 {
    font-size: 2rem;
    font-weight: 400;
    color: #fff;
    text-align: center;
  }
  img {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    z-index: 0;
  }
  .card {
    position: absolute;
    top: 40px;
    left: 40px;
    width: 50%;
    padding: 20px;
    /* height: 80%; */
    background-color: rgba(0, 0, 0, 0.5);
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    z-index: 1;
  }
  .card::before {
    content: "";
    position: absolute;
    top: -2px;
    left: -2px;
    width: calc(100% + 4px);
    height: calc(100% + 4px);
    background-color: rgba(0, 0, 0, 0.5);
    z-index: -1;
    border-radius: 8px;
    backdrop-filter: blur(10px);
  }
</style>
